extends ../../../src/views/base.pug

block prepend title
	- title = "Roles"
	- heading = title
	- menu = "roles"

block buttons
	if currentUserCan('roles_create')
		+btn('sm', 'outline-success', 'plus', 'Create Role', '/roles/create')
	+search

block contents
	include partials/list.pug
