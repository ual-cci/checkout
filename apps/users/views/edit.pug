extends ../../../src/views/base.pug

block prepend title
	- title = 'Edit User'
	- breadcrumb.push({name: user.name, url: `/users/${user.id}`})
	- breadcrumb.push({name: 'Edit'})
	- menu = 'users'

block contents
	- action = 'Update'
	include partials/form.pug
