if groups.length > 0
	.table-responsive
		table.table.table-striped
			thead
				tr
					th Name
			tbody
				for group in groups
					tr
						td: include group.pug
else
	+messageBlock( 'warning', 'There are no groups to display.' )
